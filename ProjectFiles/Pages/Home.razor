@page "/"
@page "/{contentId}"

<HeroSection />
<AboutSection ContentId="@contentId" />
<SkillsSection ContentId="@contentId" />
<ResumeSection />
<ServicesSection ContentId="@contentId" />
<PortfolioSection />
<TestimonialsSection ContentId="@contentId" />
<FaqSection ContentId="@contentId" />
<ContactSection />

@code {
	[Parameter] public string? contentId { get; set; }
}
